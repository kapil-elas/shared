import { Box } from "@mui/material";
import { styled } from "@mui/styles";

export const GreenBar = styled(Box)(({ theme }) => ({
  width: 4,
  height: "20px",
  backgroundColor: 'green',
  borderRadius: '20px',
  marginRight: '10px',
}));
