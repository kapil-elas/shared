import { Box } from "@mui/material";
import { styled } from "@mui/styles";
import colors from "@elas/shared/assets/base/colors";

export const RemoveIcon = styled(Box)(({ theme }) => ({
  width: 0,
  height: "20px",
  backgroundColor: 'blue',
  borderRadius: '20px',
  marginRight: '10px',
}));
